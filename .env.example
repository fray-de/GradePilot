# Copy to .env and fill in real values. .env is gitignored.

# OpenAI-compatible chat/VLM endpoint API key.
# The variable NAME here must match config.yaml -> llm.api_key_env.
LLM_API_KEY=

# Optional: separate key if your OCR engine uses a different provider.
# OCR_API_KEY=
